/*
 * SyncLabel Messages
 *
 * This contains all the text for the SyncLabel component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'loyaltyPlus.components.molecules.SyncLabel';

export default defineMessages({
  processing: {
    id: `${scope}.processing`,
    defaultMessage: 'Processing',
  },
});
