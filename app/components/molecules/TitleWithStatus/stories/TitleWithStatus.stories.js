import React from 'react';
import { storiesOf } from '@storybook/react';
import TitleWithStatus from '../TitleWithStatus';

storiesOf('TitleWithStatus', module).add('default', () => <TitleWithStatus />);
