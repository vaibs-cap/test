import { css } from 'styled-components';
import * as styledVars from '@capillarytech/cap-ui-library/styled/variables';

const { CAP_SPACE_20 } = styledVars;

export default css`
  .avatar-icon {
    margin-right: ${CAP_SPACE_20};
  }
`;
