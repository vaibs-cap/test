import { css } from 'styled-components';
import * as styledVars from '@capillarytech/cap-ui-library/styled/variables';

const { FONT_COLOR_04 } = styledVars;

export default css`
  .navigation-setting-icon {
    color: ${FONT_COLOR_04};
  }
`;
