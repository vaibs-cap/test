// import { createSelector } from "reselect";
// import { fromJS } from "immutable";

// const selectExpensesGraphState = (state = fromJS({})) => state.get("expenses", fromJS({}));

// export const makeExpensesGraphSelector = () =>
//     createSelector(selectExpensesGraphState, (substate = fromJS({})) =>
//         substate.get("expenses").toJS()
//     );

// export const makeLoadingGraphSelector = () =>
//     createSelector(selectExpensesGraphState, (substate = fromJS({})) =>
//         substate.get("loading")
//     );
