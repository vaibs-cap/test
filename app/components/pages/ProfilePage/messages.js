import { defineMessages } from 'react-intl';

export const scope = 'loyaltyPlus.components.pages.ProfilePage';

export default defineMessages({
  sampleHeader: {
    id: `${scope}.sampleHeader`,
    defaultMessage: 'Profle Page pod 3',
  },
});
