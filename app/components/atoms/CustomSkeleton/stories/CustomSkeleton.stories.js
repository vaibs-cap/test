import React from 'react';
import { storiesOf } from '@storybook/react';
import CustomSkeleton from '../CustomSkeleton';

storiesOf('CustomSkeleton', module).add('default', () => <CustomSkeleton />);
