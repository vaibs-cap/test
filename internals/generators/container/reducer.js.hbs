/*
 *
 * {{ properCase name }} reducer
 *
 */

import produce from 'immer';
import { DEFAULT_ACTION } from './constants';

export const initialState = {};

const {{ camelCase name }}Reducer = produce((state, action) => {
  switch (action.type) {
    case DEFAULT_ACTION:
      return state;
    default:
      return state;
  }
}, initialState);

export default {{ camelCase name }}Reducer;
